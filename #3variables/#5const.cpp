#include <iostream>

int main(){
    const double quarter = 0.25;
    // and now variable quarter can only be 0.25
    // if we try to re-assign quarter in future it will throw an err

    //quarter=09;//an err line
}