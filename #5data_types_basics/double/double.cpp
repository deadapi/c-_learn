/*
    bool is a data type used to represent boolean values.
    Values: It can only hold two values: true or false.
*/


#include <iostream>

int main() {
    bool isCodingFun = true;  // Declare a bool variable and initialize it to true
    bool isCompleted = false;     // Declare another bool variable and initialize it to false

    //as we go further we will read more about this
    return 0;
}

/*
    The double data type is used to represent floating-point numbers, 
    which are numbers that have a fractional component (decimal point).
*/

#include <iostream>

int main(){
    double c =122.2324;//supports+ve,-ve both including 0

    //variants

    /*
    float: Smaller, single-precision floating-point type.
    long double: Extended precision floating-point type. It offers even greater precision than double.
    */

   std::cout<<"Double: "<<c<<"\n";

   return 0;

}